import { FC } from 'react';
import MultiStepForm from 'components/MultiStepForm/MultiStepForm';
import AppStyles from './App.styles';
import 'normalize.css';

const App: FC = () => <MultiStepForm />;

export default AppStyles(App);
